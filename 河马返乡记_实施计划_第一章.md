# 《河马返乡记》第一章垂直切片 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出《河马返乡记》第一章「第一顿自费早餐」的可玩垂直切片——一只 deadpan 河马用蠢动作误伤敌人、被误会成英雄、解锁称号,全程零严肃且好玩到想再来一局。

**Architecture:** 纯前端、零构建。核心游戏逻辑(战斗/技能/事件/称号/扭蛋)写成纯函数 ES 模块,用 Node 内置 `node --test` 做 TDD;表现层(DOM UI + CSS juice + 粒子)用浏览器 + Playwright 截图做 playtest 验证。一切数值与内容走 `config/*.json`,美术走 `assets/` 固定文件名槽位,缺图自动 fallback 占位。

**Tech Stack:** HTML5 + 原生 ES Modules + CSS;`node --test`(内置,无依赖);Playwright(已有 webapp-testing skill)做视觉验证;JSON 配置表。

---

## 数据结构契约(全计划统一,后续任务必须沿用这些命名)

```js
// Stats —— 一个战斗单位的数值
Stats = { hp:number, maxHp:number, atk:number, def:number, weight:number, rage:number, maxRage:number }

// Combatant
Combatant = { id:string, name:string, sprite:string, isHippo:boolean, stats:Stats, statuses:object }

// Skill (config/skills.json 的一项)
Skill = { id:string, name:string, school:'nap'|'eat'|'weight'|'lazy', rarity:'common'|'rare'|'epic'|'legendary',
          desc:string, effects:object, synergyId:string|null }

// BattleState
BattleState = { round:number, maxRounds:number, hippo:Combatant, enemies:Combatant[],
                log:string[], over:boolean, win:boolean|null, dodgeWindow:boolean }

// Event (config/events.json 的一项)
Event = { id:string, need:string, text:string, npcText:string,
          choices:[ { label:string, outcome:Outcome } ] }
Outcome = { stats?:Partial<Stats>, misunderstanding?:number, log:string, nextNode?:string }

// Title (config/titles.json 的一项)
Title = { id:string, name:string, threshold:number, buff:object, sideEffect:object, npcQuip:string }

// RunState —— 一整局的状态
RunState = { misunderstanding:number, titles:string[], nodeIndex:number,
             skills:string[], equipment:object, hippo:Combatant }
```

函数命名约定(后续任务引用必须一致):
`loadConfig` · `getAsset` · `createBattle` · `stepRound` · `applyDodge` · `triggerUlt` ·
`pickSkill` · `resolveSynergy` · `resolveChoice` · `addMisunderstanding` · `checkTitleUnlock` · `spinGacha`

---

## 文件结构

```
game/
  index.html              # 入口,挂载 #app,引 main.js
  src/
    main.js               # bootstrap + 顶层状态机(节点推进)
    config.js             # loadConfig: 拉取+校验+默认值
    assets.js             # getAsset: 槽位加载,缺图 fallback 占位
    rng.js                # 可注入种子的随机(测试可复现)
    battle.js             # createBattle/stepRound/applyDodge/triggerUlt(纯逻辑)
    skills.js             # pickSkill/resolveSynergy + 效果挂载(纯逻辑)
    events.js             # resolveChoice(纯逻辑)
    titles.js             # addMisunderstanding/checkTitleUnlock(纯逻辑)
    gacha.js              # spinGacha(纯逻辑)
    ui/
      hud.js              # 顶部属性条 + 饿怒条 + 误会值条
      battleView.js       # 战斗演出 + 点击暴走/翻身
      eventView.js        # 事件文本卡 + 分支按钮
      skillChoice.js      # 三选一蠢技能
      gachaView.js        # 甜点扭蛋转盘
      titleView.js        # 称号解锁播片
      resultView.js       # 结算("晚饭呢?")+ 再来一局
    fx.js                 # juice: 顿帧/屏震/伤害飘字/粒子/squash
    audio.js              # 音效(WebAudio 合成,占位即用)
  config/
    balance.json enemies.json skills.json equipment.json
    events.json gacha.json titles.json chapter1.json
  assets/README.md        # 给美术的槽位清单
  tests/
    config.test.js battle.test.js skills.test.js
    events.test.js titles.test.js gacha.test.js
```

---

## Phase 0 — 项目骨架与配置系统

### Task 0.1: 初始化项目结构与 git
**Files:** Create `game/index.html`, `game/.gitignore`, `game/package.json`

- [ ] **Step 1: 建目录与 package.json**

`game/package.json`:
```json
{ "name": "hippo-homecoming", "version": "0.1.0", "type": "module",
  "scripts": { "test": "node --test", "serve": "python3 -m http.server 8123" } }
```

- [ ] **Step 2: 最小 index.html**

`game/index.html`:
```html
<!doctype html><html lang="zh"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1">
<title>河马返乡记</title><link rel="stylesheet" href="src/style.css"></head>
<body><div id="app"></div><script type="module" src="src/main.js"></script></body></html>
```

- [ ] **Step 3: main.js 占位**

`game/src/main.js`:
```js
const app = document.getElementById('app');
app.textContent = '河马返乡记 — 启动中…';
```

- [ ] **Step 4: 初始化 git 并提交**

Run: `cd game && git init && git add -A && git commit -m "chore: scaffold hippo-homecoming web project"`
Expected: initial commit created.

### Task 0.2: rng.js — 可复现随机
**Files:** Create `game/src/rng.js`, `game/tests/rng.test.js`

- [ ] **Step 1: 写失败测试**

`game/tests/rng.test.js`:
```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { makeRng } from '../src/rng.js';

test('same seed -> same sequence', () => {
  const a = makeRng(42), b = makeRng(42);
  assert.equal(a.next(), b.next());
  assert.equal(a.int(0,100), b.int(0,100));
});
test('int is within range', () => {
  const r = makeRng(1);
  for (let i=0;i<200;i++){ const n=r.int(0,5); assert.ok(n>=0&&n<=5); }
});
test('pick returns an element', () => {
  const r = makeRng(7);
  assert.ok(['a','b','c'].includes(r.pick(['a','b','c'])));
});
```

- [ ] **Step 2: 运行确认失败** — Run: `cd game && node --test tests/rng.test.js` — Expected: FAIL (Cannot find module rng.js)

- [ ] **Step 3: 实现 rng.js**

```js
// Mulberry32 — 确定性 PRNG
export function makeRng(seed = 1) {
  let s = seed >>> 0;
  const next = () => { s |= 0; s = s + 0x6D2B79F5 | 0;
    let t = Math.imul(s ^ s >>> 15, 1 | s);
    t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
    return ((t ^ t >>> 14) >>> 0) / 4294967296; };
  const int = (min, max) => min + Math.floor(next() * (max - min + 1));
  const pick = (arr) => arr[int(0, arr.length - 1)];
  return { next, int, pick };
}
```

- [ ] **Step 4: 运行确认通过** — Run: `cd game && node --test tests/rng.test.js` — Expected: PASS (3 tests)
- [ ] **Step 5: 提交** — `git add -A && git commit -m "feat: add seedable rng"`

### Task 0.3: config.js — 配置加载/校验/默认值
**Files:** Create `game/src/config.js`, `game/config/balance.json`, `game/tests/config.test.js`

- [ ] **Step 1: 写 balance.json(初值)**

`game/config/balance.json`:
```json
{ "maxRounds": 15, "ultRageCost": 100, "dodgeWindowMs": 700,
  "weightToDamage": 0.5, "hitstopMs": 90, "shakePx": 8,
  "hippoStart": { "hp": 360, "maxHp": 360, "atk": 40, "def": 10, "weight": 200, "rage": 0, "maxRage": 100 } }
```

- [ ] **Step 2: 写失败测试**

`game/tests/config.test.js`:
```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { validateBalance } from '../src/config.js';

test('valid balance passes', () => {
  const b = { maxRounds:15, ultRageCost:100, dodgeWindowMs:700, weightToDamage:0.5,
    hitstopMs:90, shakePx:8, hippoStart:{hp:360,maxHp:360,atk:40,def:10,weight:200,rage:0,maxRage:100} };
  assert.deepEqual(validateBalance(b).errors, []);
});
test('missing field is reported', () => {
  const { errors } = validateBalance({ maxRounds: 15 });
  assert.ok(errors.length > 0);
  assert.ok(errors.some(e => e.includes('ultRageCost')));
});
```

- [ ] **Step 3: 运行确认失败** — Run: `cd game && node --test tests/config.test.js` — Expected: FAIL

- [ ] **Step 4: 实现 config.js**

```js
export function validateBalance(b) {
  const errors = [];
  const required = ['maxRounds','ultRageCost','dodgeWindowMs','weightToDamage','hitstopMs','shakePx','hippoStart'];
  for (const k of required) if (b[k] === undefined) errors.push(`balance missing: ${k}`);
  if (b.hippoStart) for (const k of ['hp','maxHp','atk','def','weight','maxRage'])
    if (b.hippoStart[k] === undefined) errors.push(`hippoStart missing: ${k}`);
  return { errors, value: b };
}

// 浏览器侧使用:fetch + 校验,坏表则 console.error 但不崩
export async function loadConfig(name) {
  const res = await fetch(`config/${name}.json`);
  if (!res.ok) throw new Error(`config ${name} not found`);
  return res.json();
}
```

- [ ] **Step 5: 运行确认通过** — Run: `cd game && node --test tests/config.test.js` — Expected: PASS
- [ ] **Step 6: 提交** — `git add -A && git commit -m "feat: config loader + balance validation"`

### Task 0.4: assets.js — 美术槽位 + 缺图 fallback
**Files:** Create `game/src/assets.js`, `game/assets/README.md`

- [ ] **Step 1: assets/README.md(给美术的清单)**

写入固定文件名槽位表(画布 592×1280,纯2D平涂):
`hippo_idle/sit/sneeze/roll/ult/sleep.png` · `enemy_thief.png enemy_thief2.png enemy_fakehero.png` ·
`bg_diner.png bg_warehouse.png` · `item_pillow.png item_tray.png item_umbrella.png item_bread.png` ·
`ui_btn_green/red/gold.png ui_panel.png` · `icon_hp/atk/def/weight/rage/fame.png` · `wheel.png wheel_pin.png`
并注明:缺任意一张,游戏用 emoji/色块占位,不崩。

- [ ] **Step 2: 实现 assets.js**

```js
// 缺图 fallback:返回一个带 emoji 的占位 data-URI 或标记
const FALLBACK_EMOJI = {
  hippo_idle:'🦛', hippo_sit:'🦛', hippo_sneeze:'🦛', hippo_roll:'🦛', hippo_ult:'🦛', hippo_sleep:'😴',
  enemy_thief:'🦝', enemy_thief2:'🦝', enemy_fakehero:'🦸',
  item_pillow:'🛏️', item_tray:'🍽️', item_umbrella:'☂️', item_bread:'🥖', default:'❓'
};
const cache = new Map();
export function getAsset(key) {
  if (cache.has(key)) return cache.get(key);
  const img = new Image();
  let resolved = { kind:'emoji', value: FALLBACK_EMOJI[key] || FALLBACK_EMOJI.default, key };
  img.onload = () => { resolved.kind = 'img'; resolved.value = img.src; };
  img.onerror = () => {}; // 保持 emoji 占位
  img.src = `assets/${key}.png`;
  cache.set(key, resolved);
  return resolved;
}
```

- [ ] **Step 3: 提交** — `git add -A && git commit -m "feat: asset slot loader with emoji fallback + asset README for art"`

---

## Phase 1 — 战斗引擎(纯逻辑,TDD 重头戏 = 手感的数值底座)

### Task 1.1: createBattle + 伤害公式 + 体重
**Files:** Create `game/src/battle.js`, `game/config/enemies.json`, `game/tests/battle.test.js`

- [ ] **Step 1: enemies.json**

```json
{ "thief":   { "id":"thief","name":"溜门小盗","sprite":"enemy_thief","stats":{"hp":80,"maxHp":80,"atk":18,"def":4,"weight":40,"rage":0,"maxRage":100} },
  "thief2":  { "id":"thief2","name":"惯偷老鼠","sprite":"enemy_thief2","stats":{"hp":110,"maxHp":110,"atk":22,"def":6,"weight":45,"rage":0,"maxRage":100} },
  "fakehero":{ "id":"fakehero","name":"慕名挑战的冒险者","sprite":"enemy_fakehero","stats":{"hp":200,"maxHp":200,"atk":30,"def":10,"weight":70,"rage":0,"maxRage":100} } }
```

- [ ] **Step 2: 写失败测试**

`game/tests/battle.test.js`:
```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { createBattle, damage } from '../src/battle.js';

const hippo = { id:'hippo', name:'河马', isHippo:true, sprite:'hippo_idle',
  stats:{hp:360,maxHp:360,atk:40,def:10,weight:200,rage:0,maxRage:100}, statuses:{} };
const thief = { id:'thief', name:'小盗', isHippo:false, sprite:'enemy_thief',
  stats:{hp:80,maxHp:80,atk:18,def:4,weight:40,rage:0,maxRage:100}, statuses:{} };

test('damage = atk - def, min 1', () => {
  assert.equal(damage({atk:40}, {def:10}, {weightToDamage:0}), 30);
  assert.equal(damage({atk:5}, {def:99}, {weightToDamage:0}), 1);
});
test('weight adds to hippo damage', () => {
  assert.equal(damage({atk:40,weight:200}, {def:10}, {weightToDamage:0.5}), 30 + 100);
});
test('createBattle sets round 1 and not over', () => {
  const b = createBattle(hippo, [thief], { maxRounds:15, weightToDamage:0.5 });
  assert.equal(b.round, 1); assert.equal(b.over, false);
});
```

- [ ] **Step 3: 运行确认失败** — `cd game && node --test tests/battle.test.js` — Expected: FAIL

- [ ] **Step 4: 实现 createBattle + damage**

```js
export function damage(attacker, defender, balance) {
  const w = (attacker.weight || 0) * (balance.weightToDamage || 0);
  return Math.max(1, Math.round((attacker.atk || 0) + w - (defender.def || 0)));
}
export function createBattle(hippo, enemies, balance) {
  return { round:1, maxRounds: balance.maxRounds, hippo: clone(hippo),
    enemies: enemies.map(clone), log:[], over:false, win:null, dodgeWindow:false, balance };
}
const clone = (c) => JSON.parse(JSON.stringify(c));
```

- [ ] **Step 5: 运行确认通过** — Expected: PASS
- [ ] **Step 6: 提交** — `git commit -am "feat: battle creation + weight-aware damage formula"`

### Task 1.2: stepRound — 自动回合(河马蠢动作普攻 + 敌人攻击 + 胜负)
**Files:** Modify `game/src/battle.js`, `game/tests/battle.test.js`

- [ ] **Step 1: 追加失败测试**

```js
import { stepRound } from '../src/battle.js';
test('stepRound: hippo auto-attacks, enemy loses hp', () => {
  const b = createBattle(hippo, [thief], { maxRounds:15, weightToDamage:0.5 });
  const before = b.enemies[0].stats.hp;
  stepRound(b);
  assert.ok(b.enemies[0].stats.hp < before);
  assert.equal(b.round, 2);
});
test('stepRound: win when all enemies dead', () => {
  const weak = {...thief, stats:{...thief.stats, hp:1}};
  const b = createBattle(hippo, [weak], { maxRounds:15, weightToDamage:0.5 });
  stepRound(b);
  assert.equal(b.over, true); assert.equal(b.win, true);
});
test('stepRound: rage builds on attack', () => {
  const b = createBattle(hippo, [thief], { maxRounds:15, weightToDamage:0.5 });
  stepRound(b);
  assert.ok(b.hippo.stats.rage > 0);
});
test('stepRound: lose if maxRounds exceeded and enemies alive', () => {
  const tank = {...thief, stats:{...thief.stats, hp:99999}};
  const b = createBattle(hippo, [tank], { maxRounds:1, weightToDamage:0.5 });
  stepRound(b);
  assert.equal(b.over, true); assert.equal(b.win, false);
});
```

- [ ] **Step 2: 运行确认失败** — Expected: FAIL (stepRound undefined)

- [ ] **Step 3: 实现 stepRound**

```js
const HIPPO_MOVES = ['一屁股坐下','打了个喷嚏','翻了个身','顺手抢了口吃的'];
export function stepRound(b) {
  if (b.over) return b;
  // 河马普攻第一个活着的敌人(被动蠢动作)
  const target = b.enemies.find(e => e.stats.hp > 0);
  if (target) {
    const move = HIPPO_MOVES[(b.round - 1) % HIPPO_MOVES.length];
    const dmg = damage(b.hippo.stats, target.stats, b.balance);
    target.stats.hp = Math.max(0, target.stats.hp - dmg);
    b.hippo.stats.rage = Math.min(b.hippo.stats.maxRage, b.hippo.stats.rage + 25);
    b.log.push(`河马${move},${target.name} -${dmg}`);
  }
  // 敌人反击
  for (const e of b.enemies) {
    if (e.stats.hp <= 0) continue;
    const dmg = damage(e.stats, b.hippo.stats, b.balance);
    b.hippo.stats.hp = Math.max(0, b.hippo.stats.hp - dmg);
    b.log.push(`${e.name}偷袭,河马 -${dmg}`);
  }
  // 胜负
  if (b.enemies.every(e => e.stats.hp <= 0)) { b.over = true; b.win = true; }
  else if (b.hippo.stats.hp <= 0) { b.over = true; b.win = false; }
  else if (b.round >= b.maxRounds) { b.over = true; b.win = false; }
  else b.round++;
  return b;
}
```

- [ ] **Step 4: 运行确认通过** — Expected: PASS (all battle tests)
- [ ] **Step 5: 提交** — `git commit -am "feat: stepRound auto-battle with rage, win/lose, round cap"`

### Task 1.3: triggerUlt(暴走)+ applyDodge(翻个身)
**Files:** Modify `game/src/battle.js`, `game/tests/battle.test.js`

- [ ] **Step 1: 追加失败测试**

```js
import { triggerUlt, applyDodge } from '../src/battle.js';
test('triggerUlt needs full rage, hits all, resets rage', () => {
  const b = createBattle(hippo, [thief, {...thief, id:'t2'}], { maxRounds:15, weightToDamage:0.5, ultRageCost:100 });
  b.hippo.stats.rage = 100;
  const ok = triggerUlt(b);
  assert.equal(ok, true);
  assert.equal(b.hippo.stats.rage, 0);
  assert.ok(b.enemies.every(e => e.stats.hp < e.stats.maxHp));
});
test('triggerUlt fails without full rage', () => {
  const b = createBattle(hippo, [thief], { maxRounds:15, weightToDamage:0.5, ultRageCost:100 });
  b.hippo.stats.rage = 50;
  assert.equal(triggerUlt(b), false);
});
test('applyDodge negates next enemy hit this round', () => {
  const b = createBattle(hippo, [thief], { maxRounds:15, weightToDamage:0.5 });
  applyDodge(b);
  const hpBefore = b.hippo.stats.hp;
  stepRound(b);
  // 翻身闪避:河马这回合不掉血(被动普攻仍打出)
  assert.equal(b.hippo.stats.hp, hpBefore);
});
```

- [ ] **Step 2: 运行确认失败** — Expected: FAIL

- [ ] **Step 3: 实现(并在 stepRound 中接 dodge 标志)**

在 `stepRound` 敌人反击循环开头加:`if (b.dodgeWindow) { b.log.push('河马睡梦中翻了个身,正好躲过'); b.dodgeWindow=false; break; }`

新增:
```js
export function triggerUlt(b) {
  if (b.hippo.stats.rage < (b.balance.ultRageCost || 100)) return false;
  b.hippo.stats.rage = 0;
  for (const e of b.enemies) {
    if (e.stats.hp <= 0) continue;
    const dmg = damage(b.hippo.stats, e.stats, b.balance) * 2; // 被吵醒暴走
    e.stats.hp = Math.max(0, e.stats.hp - dmg);
  }
  b.log.push('河马被彻底吵醒,短暂暴走!');
  if (b.enemies.every(e => e.stats.hp <= 0)) { b.over = true; b.win = true; }
  return true;
}
export function applyDodge(b) { b.dodgeWindow = true; }
```

- [ ] **Step 4: 运行确认通过** — Expected: PASS
- [ ] **Step 5: 提交** — `git commit -am "feat: ultimate (饿怒暴走) + dodge (翻个身闪避)"`

---

## Phase 2 — 蠢技能系统 + 协同(纯逻辑)

### Task 2.1: skills.json + pickSkill(三选一抽取)
**Files:** Create `game/src/skills.js`, `game/config/skills.json`, `game/tests/skills.test.js`

- [ ] **Step 1: skills.json(≥8,覆盖4流派,名字用文档原话)**

```json
[
 {"id":"sleep5","name":"再睡五分钟","school":"nap","rarity":"common","desc":"装睡:有概率免疫一次攻击","effects":{"dodgeChance":0.25},"synergyId":"nap"},
 {"id":"donttouch","name":"别碰我的枕头","school":"nap","rarity":"rare","desc":"被打后反弹部分伤害","effects":{"thorns":0.3},"synergyId":"nap"},
 {"id":"notfree","name":"这份不是免费的吗","school":"eat","rarity":"common","desc":"每回合吃场上食物回血","effects":{"healPerRound":20},"synergyId":"eat"},
 {"id":"breakfast","name":"早餐时间","school":"eat","rarity":"rare","desc":"吃饱后攻击提升","effects":{"atkOnHeal":6},"synergyId":"eat"},
 {"id":"weightadv","name":"体重优势","school":"weight","rarity":"common","desc":"体重转化伤害提升","effects":{"weightBonus":0.3},"synergyId":"weight"},
 {"id":"sitdown","name":"坐一下","school":"weight","rarity":"rare","desc":"暴走波及全场","effects":{"ultSplash":true},"synergyId":"weight"},
 {"id":"notoday","name":"今天不上班","school":"lazy","rarity":"common","desc":"不主动时积累反伤","effects":{"lazyThorns":0.2},"synergyId":"lazy"},
 {"id":"ididntagree","name":"我没答应","school":"lazy","rarity":"rare","desc":"拒绝第一次致命伤","effects":{"denyDeath":true},"synergyId":"lazy"}
]
```

- [ ] **Step 2: 写失败测试**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { makeRng } from '../src/rng.js';
import { pickSkill } from '../src/skills.js';
const pool = JSON.parse(await (await import('node:fs/promises')).readFile(new URL('../config/skills.json', import.meta.url)));

test('pickSkill returns 3 distinct skills', () => {
  const r = makeRng(3);
  const three = pickSkill(pool, [], r);
  assert.equal(three.length, 3);
  assert.equal(new Set(three.map(s=>s.id)).size, 3);
});
test('pickSkill excludes already-owned', () => {
  const r = makeRng(3);
  const owned = pool.slice(0,5).map(s=>s.id);
  const three = pickSkill(pool, owned, r);
  assert.ok(three.every(s => !owned.includes(s.id)));
});
```

- [ ] **Step 3: 运行确认失败** — Expected: FAIL

- [ ] **Step 4: 实现 pickSkill**

```js
export function pickSkill(pool, ownedIds, rng) {
  const avail = pool.filter(s => !ownedIds.includes(s.id));
  const out = [];
  const bag = [...avail];
  while (out.length < 3 && bag.length) {
    const i = rng.int(0, bag.length - 1);
    out.push(bag.splice(i, 1)[0]);
  }
  return out;
}
```

- [ ] **Step 5: 运行确认通过** — Expected: PASS
- [ ] **Step 6: 提交** — `git commit -am "feat: skill pool + three-choice draw"`

### Task 2.2: resolveSynergy(同流派质变)
**Files:** Modify `game/src/skills.js`, `game/tests/skills.test.js`

- [ ] **Step 1: 追加失败测试**

```js
import { resolveSynergy } from '../src/skills.js';
test('3 same-school skills trigger synergy', () => {
  const owned = [{synergyId:'nap'},{synergyId:'nap'},{synergyId:'nap'}];
  const s = resolveSynergy(owned);
  assert.deepEqual(s, ['nap']);
});
test('mixed schools -> no synergy', () => {
  const owned = [{synergyId:'nap'},{synergyId:'eat'},{synergyId:'weight'}];
  assert.deepEqual(resolveSynergy(owned), []);
});
```

- [ ] **Step 2: 运行确认失败** — Expected: FAIL

- [ ] **Step 3: 实现**

```js
export function resolveSynergy(ownedSkills, threshold = 3) {
  const counts = {};
  for (const s of ownedSkills) if (s.synergyId) counts[s.synergyId] = (counts[s.synergyId]||0)+1;
  return Object.keys(counts).filter(k => counts[k] >= threshold);
}
```

- [ ] **Step 4: 运行确认通过** — Expected: PASS
- [ ] **Step 5: 提交** — `git commit -am "feat: skill school synergy resolution"`

---

## Phase 3 — 事件系统(纯逻辑)

### Task 3.1: events.json(第一章 6 事件)+ resolveChoice
**Files:** Create `game/src/events.js`, `game/config/events.json`, `game/tests/events.test.js`

- [ ] **Step 1: events.json(deadpan 文案,扣章节模板)**

至少 6 条,示例两条:
```json
[
 {"id":"e_diner_pay","need":"吃早餐","text":"河马吃完了一整桌。老板递来账单。河马第一次知道:食物要钱。",
  "npcText":"——他面不改色,老板以为遇到了大人物。",
  "choices":[
    {"label":"洗盘子抵债","outcome":{"misunderstanding":10,"log":"河马手太大,把盘子全捏碎了。","nextNode":"battle_warehouse"}},
    {"label":"假装没看见,走出去","outcome":{"stats":{"def":-2},"misunderstanding":25,"log":"河马淡定走出门。老板:‘高人不屑结账……’","nextNode":"battle_warehouse"}}
  ]},
 {"id":"e_warehouse_bread","need":"找剩下的面包","text":"被罚去仓库搬货。河马只想找点剩面包。货堆后面有窸窸窣窣的声音。",
  "npcText":"——他径直走过去,镇民后来说他是‘嗅到了罪恶’。",
  "choices":[
    {"label":"扒开货堆找面包","outcome":{"misunderstanding":20,"log":"河马一扒,盗贼窝塌了。","nextNode":"battle_thieves"}},
    {"label":"先睡一会再说","outcome":{"stats":{"rage":50},"misunderstanding":5,"log":"河马靠着货堆睡着,鼾声吓跑半数盗贼。","nextNode":"battle_thieves"}}
  ]}
]
```

- [ ] **Step 2: 写失败测试**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { resolveChoice } from '../src/events.js';

test('resolveChoice applies stat delta and misunderstanding', () => {
  const run = { misunderstanding:0, hippo:{stats:{def:10,rage:0}} };
  const choice = { label:'x', outcome:{ stats:{def:-2}, misunderstanding:25, log:'ok', nextNode:'n1' } };
  const r = resolveChoice(run, choice);
  assert.equal(run.hippo.stats.def, 8);
  assert.equal(run.misunderstanding, 25);
  assert.equal(r.nextNode, 'n1');
});
test('resolveChoice clamps stats at 0', () => {
  const run = { misunderstanding:0, hippo:{stats:{def:1}} };
  resolveChoice(run, { outcome:{ stats:{def:-5}, misunderstanding:0, log:'' } });
  assert.equal(run.hippo.stats.def, 0);
});
```

- [ ] **Step 3: 运行确认失败** — Expected: FAIL

- [ ] **Step 4: 实现 resolveChoice**

```js
export function resolveChoice(run, choice) {
  const o = choice.outcome || {};
  if (o.stats) for (const [k,v] of Object.entries(o.stats))
    run.hippo.stats[k] = Math.max(0, (run.hippo.stats[k]||0) + v);
  if (o.misunderstanding) run.misunderstanding += o.misunderstanding;
  return { log: o.log || '', nextNode: o.nextNode || null };
}
```

- [ ] **Step 5: 运行确认通过** — Expected: PASS
- [ ] **Step 6: 提交** — `git commit -am "feat: event choice resolution + chapter1 events"`

---

## Phase 4 — 误会→名声→称号 meta(纯逻辑,游戏的灵魂)

### Task 4.1: titles.json + addMisunderstanding + checkTitleUnlock
**Files:** Create `game/src/titles.js`, `game/config/titles.json`, `game/tests/titles.test.js`

- [ ] **Step 1: titles.json(第一章 2 个 + 后续占位)**

```json
[
 {"id":"freeloader","name":"霸王餐客人","threshold":20,"buff":{"atk":4},"sideEffect":{"note":"老板们开始记住你的脸"},"npcQuip":"‘那位大人物,从不带钱——真有派头。’"},
 {"id":"breadguardian","name":"面包街守护者","threshold":50,"buff":{"healPerRound":10},"sideEffect":{"extraEnemy":"fakehero"},"npcQuip":"‘面包街的守护者!’(其实他只是在找剩面包)"}
]
```

- [ ] **Step 2: 写失败测试**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { addMisunderstanding, checkTitleUnlock } from '../src/titles.js';
const titles = JSON.parse(await (await import('node:fs/promises')).readFile(new URL('../config/titles.json', import.meta.url)));

test('crossing threshold unlocks a title once', () => {
  const run = { misunderstanding:0, titles:[] };
  addMisunderstanding(run, 25);
  const newly = checkTitleUnlock(run, titles);
  assert.deepEqual(newly.map(t=>t.id), ['freeloader']);
  // 再查不重复解锁
  assert.deepEqual(checkTitleUnlock(run, titles), []);
});
test('big gain can unlock multiple', () => {
  const run = { misunderstanding:0, titles:[] };
  addMisunderstanding(run, 60);
  const newly = checkTitleUnlock(run, titles);
  assert.deepEqual(newly.map(t=>t.id).sort(), ['breadguardian','freeloader']);
});
```

- [ ] **Step 3: 运行确认失败** — Expected: FAIL

- [ ] **Step 4: 实现**

```js
export function addMisunderstanding(run, amount) {
  run.misunderstanding = (run.misunderstanding||0) + amount;
  return run.misunderstanding;
}
export function checkTitleUnlock(run, titles) {
  run.titles = run.titles || [];
  const newly = titles.filter(t => run.misunderstanding >= t.threshold && !run.titles.includes(t.id));
  for (const t of newly) run.titles.push(t.id);
  return newly;
}
```

- [ ] **Step 5: 运行确认通过** — Expected: PASS
- [ ] **Step 6: 提交** — `git commit -am "feat: misunderstanding->fame->title meta system"`

---

## Phase 5 — 甜点扭蛋(纯逻辑)

### Task 5.1: gacha.json + spinGacha(带权)
**Files:** Create `game/src/gacha.js`, `game/config/gacha.json`, `game/tests/gacha.test.js`

- [ ] **Step 1: gacha.json**

```json
{ "segments":[
  {"id":"exp","label":"经验+100","weight":30,"reward":{"exp":100}},
  {"id":"atk","label":"攻击+8","weight":20,"reward":{"stats":{"atk":8}}},
  {"id":"heal","label":"回满","weight":20,"reward":{"healFull":true}},
  {"id":"skill","label":"随机技能","weight":15,"reward":{"skill":true}},
  {"id":"dessert","label":"一块甜点(并没有用)","weight":15,"reward":{"flavor":"河马很满意,但还是没吃到正餐。"}}
]}
```

- [ ] **Step 2: 写失败测试**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { makeRng } from '../src/rng.js';
import { spinGacha } from '../src/gacha.js';
const cfg = JSON.parse(await (await import('node:fs/promises')).readFile(new URL('../config/gacha.json', import.meta.url)));

test('spinGacha returns a valid segment', () => {
  const r = makeRng(9);
  const seg = spinGacha(cfg, r);
  assert.ok(cfg.segments.find(s => s.id === seg.id));
});
test('weighting: exp (highest weight) appears most over many spins', () => {
  const r = makeRng(123); const counts={};
  for (let i=0;i<2000;i++){ const s=spinGacha(cfg,r); counts[s.id]=(counts[s.id]||0)+1; }
  const top = Object.entries(counts).sort((a,b)=>b[1]-a[1])[0][0];
  assert.equal(top, 'exp');
});
```

- [ ] **Step 3: 运行确认失败** — Expected: FAIL

- [ ] **Step 4: 实现 spinGacha**

```js
export function spinGacha(cfg, rng) {
  const total = cfg.segments.reduce((a,s)=>a+s.weight,0);
  let roll = rng.next() * total;
  for (const s of cfg.segments) { if ((roll -= s.weight) < 0) return s; }
  return cfg.segments[cfg.segments.length-1];
}
```

- [ ] **Step 5: 运行确认通过** — Expected: PASS
- [ ] **Step 6: 提交** — `git commit -am "feat: weighted dessert gacha"`

---

## Phase 6 — 表现层(DOM UI,playtest 验证)

> 这些任务无单测,验证方式 = 起本地服务 + Playwright 截图 + 人眼检查。每个任务结束 commit。

### Task 6.1: style.css + 竖屏舞台骨架
**Files:** Create `game/src/style.css`, modify `game/src/main.js`
- [ ] **Step 1:** 实现 592×1280 居中竖屏容器(响应式缩放 `scale` 适配手机/桌面),三段式布局:顶部 HUD / 中部舞台 / 底部主按钮区。暖橙+森林绿+奶白主色(可被美术主色覆盖)。
- [ ] **Step 2:** 起服务截图验证:`cd game && python3 -m http.server 8123`,用 webapp-testing 打开 `http://localhost:8123` 截图,确认竖屏骨架与配色。
- [ ] **Step 3:** 提交 — `git commit -am "feat: portrait stage layout + base theme"`

### Task 6.2: hud.js(属性条 + 饿怒条 + 误会值条)
**Files:** Create `game/src/ui/hud.js`
- [ ] 渲染 HP/攻击/防御/体重 + 饿怒条(满了高亮可点)+ 误会值条(接近称号阈值时闪烁)。图标走 `getAsset`,缺图用 emoji。截图验证。提交。

### Task 6.3: battleView.js(战斗演出 + 点击暴走/翻身)
**Files:** Create `game/src/ui/battleView.js`
- [ ] 河马 vs 敌人横向布局、血条、回合数、底部【战斗中】。普攻轮播河马蠢动作精灵(sit/sneeze/roll)。饿怒满时底部按钮变【暴走】可点 → `triggerUlt`。敌人出手前 700ms 出现可点区域 → `applyDodge`(翻个身)。截图验证一帧战斗 + 一帧暴走。提交。

### Task 6.4: eventView / skillChoice / gachaView / titleView / resultView
**Files:** Create 对应 5 个 `game/src/ui/*.js`
- [ ] eventView:事件文本卡 + deadpan 分支按钮。
- [ ] skillChoice:三选一卡片(流派色 + 稀有度边框 + 文档原话技能名)。
- [ ] gachaView:转盘 + 蓄力减速 + 中奖爆光。
- [ ] titleView:称号解锁播片(NPC 脑补台词 `npcQuip`)。
- [ ] resultView:结算面板,全世界欢呼 + 河马唯一台词 **"晚饭呢?"** + 【再来一局】。
- [ ] 每个做完截图验证 + 提交。

---

## Phase 7 — Juice 与音效(playtest 验证,这是"惊艳"的关键)

### Task 7.1: fx.js
**Files:** Create `game/src/fx.js`
- [ ] 实现:`hitstop(ms)`(短暂冻结 rAF)、`shake(px)`(舞台 transform 抖动)、`popDamage(x,y,n)`(伤害数字弹跳放大)、`particles(x,y,kind)`(灰尘/星星)、`squash(el)`(敌人被压扁回弹)。参数读 `balance.json`。
- [ ] 在 battleView 命中/暴走/击杀时调用。截图 + 录屏验证"一屁股坐下压扁盗贼"的爽感。提交。

### Task 7.2: audio.js
**Files:** Create `game/src/audio.js`
- [ ] WebAudio 合成占位音效:坐下"砰"、喷嚏、连击升调、暴走低吼、称号解锁叮。无需音频文件即可用。提交。

---

## Phase 8 — 整合:第一章完整一局

### Task 8.1: chapter1.json(节点编排)+ main.js 状态机
**Files:** Create `game/config/chapter1.json`, finalize `game/src/main.js`
- [ ] chapter1.json 把事件/战斗/三选一/扭蛋/称号节点串成第一章流程(对应 spec 第4节六步)。
- [ ] main.js 实现顶层状态机:`event → battle → skillChoice → (gacha) → titleCheck → nextNode → result`,串起所有纯逻辑模块 + UI。
- [ ] 起服务,用 Playwright 完整走一遍:进餐馆→洗盘子→仓库→打盗贼(看到坐下压扁)→解锁"面包街守护者"→结算"早餐呢?"。截图每个关键屏。提交。

### Task 8.2: 平衡过一遍
**Files:** Modify `game/config/balance.json`, `enemies.json`
- [ ] 按 `/balance-check` 思路核对 TTK:小盗贼应 2–3 回合倒,章节高潮敌人 5–8 回合,一局时长 3–5 分钟。调 `balance.json` 数值(不改代码)。提交。

---

## Phase 9 — 好玩验收(对照 spec 第8节)

- [ ] 起服务,完整 playtest 录屏。逐条核对:坐下压扁会心一笑 / 称号 punchline 立住 / juice 让"啥没干却赢"也爽 / 通关想再来一局 / 全程零严肃。
- [ ] 用已装 `/playtest-report` skill 思路写一份简短 playtest 记录,列出不好玩的点 → 进下一轮迭代清单。
- [ ] 最终提交 + 在 `game/README.md` 写"怎么跑"(`python3 -m http.server 8123` 然后开 localhost:8123)。

---

## 自查(Self-Review)结果
- **Spec 覆盖**:战斗(P1)/技能协同(P2)/事件(P3)/称号meta(P4)/扭蛋(P5)/UI(P6)/juice(P7)/第一章整合(P8)/好玩验收(P9) —— spec 各系统均有对应任务。✓
- **无占位**:核心逻辑任务均含真实测试码与实现码;UI/juice 任务标注 playtest 验证方式(游戏手感不适合单测)。✓
- **类型一致**:`Stats/Combatant/Skill/Event/Title/RunState` 字段与 `createBattle/stepRound/triggerUlt/applyDodge/pickSkill/resolveSynergy/resolveChoice/addMisunderstanding/checkTitleUnlock/spinGacha` 命名在各任务间一致。✓
</content>
