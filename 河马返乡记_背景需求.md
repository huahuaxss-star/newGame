# 《河马返乡记》背景图需求

> 现状：背景是 3 层视差无限横向滚动（远景天际线 / 中景街区 / 近景路面）。问题：中景只有「早点铺街区」一张，一直循环很枯燥。
> 目标：**每层多做几张可交替**的图，按场景/章节随机或顺序切换，让"一路上风景在变"。

## 硬性规格（所有背景层通用）
- **横向无缝循环**：左右边缘必须**首尾一致**（拼接处无缝），这样才能无限滚动不跳帧。
- 画布建议 **2172 × 724**（与现有素材一致），透明或不透明见各层说明。
- 纯 2D 平涂、暖色、低饱和、与现有风格统一。
- 命名见下方"文件名"，丢进 `游戏/assets/` 或 `美术/地图场景/` 我来接。

## 三层分别要什么

### ① 中景 / 街区层（最需要！至少再做 3-4 张）—— **背景透明**（只画建筑，天空留空）
玩家停留最久、最显眼。需要不同地点轮换：
| 文件名 | 场景 | 说明 |
|---|---|---|
| `mid_diner.png`（已有=早点铺） | 早点铺街区 | 现有 |
| `mid_market.png` | 集市/摊位街 | 蔬果摊、布棚、吆喝牌 |
| `mid_station.png` | 车站/候车区 | 站牌、长椅、检票口 |
| `mid_temple.png` | 神庙/古迹前 | 石阶、香炉、供品摊 |
| `mid_bath.png`（可选） | 公共浴场街 | 澡堂招牌、木桶 |
> 关键：**只画建筑/摊位这一条带**，上下都透明（天空交给远景层，地面交给路面层），这样能叠在任何远景上。

### ② 远景 / 天际线层（再做 1-2 张就够）—— **不透明**（含天空）
| 文件名 | 场景 |
|---|---|
| `far_town.png`（已有=动物小镇） | 远处小镇天际线 |
| `far_hills.png` | 郊野/山丘（树林、远山、少量屋顶） |
| `far_dusk.png`（可选） | 黄昏色调版（换氛围） |

### ③ 近景 / 路面层（再做 1-2 张就够）—— 不透明，铺满
| 文件名 | 场景 |
|---|---|
| `ground_dirt.png`（已有=沙土路） | 沙土小径 |
| `ground_stone.png` | 石板路/镇内地砖 |
| `ground_grass.png`（可选） | 草地边小路 |

## 优先级
1. **先做中景 3 张**（market / station / temple）——这是消除"一直早点铺"枯燥感的关键。
2. 再补远景 1 张（far_hills）换换天际线。
3. 路面 1 张（ground_stone）。

## 我这边怎么用
- 接好后，背景按"当前事件/地点"或随机切换中景层（带淡入过渡），远景/路面也可随章节切换。
- 滚动速度已调快到贴近河马奔跑手感；多图交替后就不会枯燥了。
